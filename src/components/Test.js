import { useState } from "react";

export default function Test() {

  const [x, setX] = useState(0);

  return(
    <div>
      <div>This is a Test (11)</div>
      <div>Just write anything here</div>
    </div>
  );
}